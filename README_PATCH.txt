V25 PATCH

Upload every file in this ZIP to the ROOT of the current Portfolio branch and overwrite existing files.

Changes:
- replaces RADIO_33 with a compact radio + separate terminal signal monitor
- adds animated fake waveform/equalizer seeded from the current Song of the Week
- best-effort YouTube play/pause sync makes the signal, speaker and dancer react when playback state is available
- fixes the bottom-left bullshark so the full sprite is visible instead of being clipped by the ocean box
- includes all SMURF screenshots and support files; SMURF gallery remains 14 images
- uses the agreed public SMURF wording without expanding the acronym
- removes every site reference to locomotive images 26-30

IMPORTANT: ZIP uploads cannot delete old GitHub files. After uploading, delete the filenames listed in DELETE_FROM_REPO.txt if they exist in the branch.
