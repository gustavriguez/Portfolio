V58 MEDIA ASSET CORRECTION

Changes from V57:
- IEEE EXO selected-work image now uses the actual GBM EXO demonstration-table photo embedded in the existing portfolio gallery.
- IEEE GBM media tile uses that same real GBM photo for consistency.
- Mime media tile now uses exactly one real Mime gallery image: the open-hand computer-vision control image.
- No generated GBM/Mime poster artwork is used by work.html anymore.

Upload all files to the GitHub Pages repository root and overwrite matching files.
